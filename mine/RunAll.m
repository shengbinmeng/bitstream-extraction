function RunAll(path, frame_num)
Run([path, 'Bus'], frame_num);
Run([path, 'City'], frame_num);
Run([path, 'Crew'], frame_num);
Run([path, 'Football'], frame_num);
Run([path, 'Foreman'], frame_num);
Run([path, 'Harbour'], frame_num);
Run([path, 'Mobile'], frame_num);
Run([path, 'Soccer'], frame_num);
end